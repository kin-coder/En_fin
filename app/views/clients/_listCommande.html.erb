<!-- Anle CLIENT CLIENT -->
<!-- <table border="2">
	<tr>
		<td>Date <small>(MM/JJ/AAAA)</small></td>
		<td>Prix</td>
		<td>Prestation</td>
		<td>Action</td>
	</tr>

	<% orders.each do |order| %>
	<% prixAcompte = order.totalPrice %>
	<tr>
		<td><%= order.prestation_date %></td>
		<td><%= prixAcompte[0] + prixAcompte[1] %> €</td>
		<td>
			<ul>
				<% order.order_services.each do |o_s| %>
					<li><%= o_s.service.name %></li>
				<% end %>
			</ul>
		</td>
		<td><a href="<%= client_order_path(order.id) %>">Afficher détails</a></td>
	</tr>
	<% end %>
</table> -->

<div class="table-responsive">
									<table class="table">
										<thead>
											<tr>
												<th>N°</th>
												<th>Date</th>
												<th>Préstation</th>
												<th>Prix total</th>
												<th>Actions</th>
											</tr>
										</thead>
										<tbody>

											<% @orders.each do |order| %>
											<% prixAcompte = order.totalPrice %>
											<tr>
												<td><%= order.id %></td>
												<td><%= order.prestation_date %></td>
												<td>
													<% order.order_services.each do |prestation| %>
													<%= prestation.service.name %> <br>
													<% end %>
												</td>
												<td><%= prixAcompte[0] + prixAcompte[1] %> €</td>
												<td><a href="<%= client_order_path(order.id) %>" class="btn btn-default" DATA-Method="GET">Afficher</a></td>

											</tr>
											<% end %>
										</tbody>
									</table>
								</div>